<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title "Joz Blog" %}</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.5/css/bootstrap.min.css"
              integrity="sha384-AysaV+vQoT3kOAXZkl02PThvDr8HYKPZhNT5h/CXfBThSRXQ6jW5DO2ekP5ViFdi" crossorigin="anonymous">
        <style>
            body {
                padding-top: 5rem;
            }
        </style>
    </head>
    <body>

        <nav class="navbar navbar-fixed-top navbar-dark bg-inverse">
            <a class="navbar-brand" href="#">Joz Blog</a>
            <ul class="nav navbar-nav">
                <li class="nav-item {{ is_subpath('blog.index') ? 'active' }}">
                    <a class="nav-link" href="{{ path('blog.index') }}">Home</a>
                </li>
                <li class="nav-item {{ is_subpath('contact') ? 'active' }}">
                    <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                </li>
            </ul>
            <div class="nav navbar-nav">
                {% if current_user() %}
                    <div class="navbar-text">Connecté en tant que {{ current_user().username }}</div>
                    <form class="nav-item active" method="post" action="{{ path('auth.logout') }}">
                        {{ csrf_input() }}
                        <button class="nav-link">Log Out</button>
                    </form>
                {% else %}
                    <div class="nav-item active">
                        <a class="nav-link" href="{{ path('auth.login') }}">Log In</a>
                    </div>
                {% endif %}
            </div>
        </nav>

        <div class="container">

            {% block body %}{% endblock%}

        </div><!-- /.container -->

        <!-- Bootstrap JS -->
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0-alpha.5/js/bootstrap.min.js"
                integrity="sha384-BLiI7JTZm+JWlgKa0M0kGRpJbF2J8q+qreVrKBC47e3K6BW78kGLrCkeRX6I9RoK"
                crossorigin="anonymous">
        </script>
    {#    <script src="https://cdnjs.cloudflare.com/ajax/libs/timeago.js/4.0.2/timeago.full.min.js"></script>#}
    {#    <script>#}
    {#        timeago().render(document.querySelectorAll('.timeago'), 'fr')#}
    {#    </script>#}

    {#    KnpTimeBundle #}
    {#    https://symfonycasts.com/screencast/symfony4-doctrine/ago-filter #}

    </body>
</html>
